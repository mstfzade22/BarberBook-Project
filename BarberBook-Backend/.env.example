# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_in_production_12345
JWT_EXPIRE=7d

# CORS
FRONTEND_URL=http://localhost:8000

# Data Storage
DATA_STORAGE=file
# Options: memory (in-memory) or file (JSON files)
